"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, RefreshCw, Target, TrendingUp } from "lucide-react";
import { createClient } from "@/lib/supabase-browser";
import { AUTO_REINVEST_RATE, DEFAULT_GOAL_TARGET, currentValue, projectGoal, recurringMonthly, isActive, type GoalActivity, type GoalInvestment } from "@/lib/goal-projection";
const money=(n:number)=>`₹${Math.round(Math.max(0,n)).toLocaleString("en-IN")}`;
const short=(n:number)=>n>=10000000?`₹${(n/10000000).toFixed(1)}Cr`:n>=100000?`₹${(n/100000).toFixed(1)}L`:money(n);
const fmt=(d:Date)=>d.toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"});
function Chart({points,target}:{points:{date:Date;value:number}[];target:number}){const W=1000,H=330,L=55,R=20,T=20,B=42,max=Math.max(target,...points.map(p=>p.value),1),pw=W-L-R,ph=H-T-B;const path=points.map((p,i)=>{const x=L+(points.length<2?pw/2:i/(points.length-1)*pw),y=T+ph-p.value/max*ph;return`${i?"L":"M"}${x},${y}`}).join(" ");const ty=T+ph-target/max*ph;return <div className="overflow-x-auto rounded-2xl bg-slate-50 p-3"><svg viewBox={`0 0 ${W} ${H}`} className="min-w-[680px] w-full"><line x1={L} x2={W-R} y1={ty} y2={ty} stroke="#7c3aed" strokeWidth="2" strokeDasharray="7 7"/><text x={W-R} y={ty-8} textAnchor="end" fontSize="12" fontWeight="700" fill="#7c3aed">Target {short(target)}</text>{[0,.25,.5,.75,1].map(q=><g key={q}><line x1={L} x2={W-R} y1={T+ph*(1-q)} y2={T+ph*(1-q)} stroke="#e5e7eb"/><text x={L-8} y={T+ph*(1-q)+4} textAnchor="end" fontSize="10" fill="#64748b">{short(max*q)}</text></g>)}<path d={path} fill="none" stroke="#111827" strokeWidth="4" strokeLinecap="round"/><text x={L} y={H-12} fontSize="11" fill="#64748b">{points[0]?.date.toLocaleDateString("en-IN",{month:"short",year:"numeric"})}</text><text x={W-R} y={H-12} textAnchor="end" fontSize="11" fill="#64748b">{points.at(-1)?.date.toLocaleDateString("en-IN",{month:"short",year:"numeric"})}</text></svg></div>}
export default function EditableGoalInsights(){const [investments,setInvestments]=useState<GoalInvestment[]>([]),[activities,setActivities]=useState<GoalActivity[]>([]),[target,setTarget]=useState(DEFAULT_GOAL_TARGET),[loading,setLoading]=useState(true),[error,setError]=useState("");const today=new Date();async function load(){setLoading(true);const s=createClient(),{data:{user}}=await s.auth.getUser();if(!user){setLoading(false);return}const[{data:inv,error:ie},{data:act,error:ae},{data:goal,error:ge}]=await Promise.all([s.from("goal_investments").select("*").eq("user_id",user.id).order("invested_on"),s.from("cash_flows").select("*").eq("user_id",user.id).eq("is_recurring",true).order("flow_date"),s.from("financial_goals").select("target_amount").eq("user_id",user.id).maybeSingle()]);if(ie||ae||ge)setError(ie?.message||ae?.message||ge?.message||"Unable to load goal data.");setInvestments((inv??[])as GoalInvestment[]);setActivities((act??[])as GoalActivity[]);setTarget(Math.max(1,Number(goal?.target_amount)||DEFAULT_GOAL_TARGET));setLoading(false)}useEffect(()=>{load()},[]);const current=useMemo(()=>investments.reduce((s,i)=>s+currentValue(i,today),0),[investments,today.toDateString()]);const surplus=Math.max(0,activities.filter(a=>isActive(a,today)).reduce((s,a)=>s+(a.flow_type==="income"?1:-1)*recurringMonthly(a),0));const points=useMemo(()=>projectGoal(investments,activities,[],today,target),[investments,activities,target,today.toDateString()]);const reach=points.find(p=>p.value>=target);const progress=Math.min(100,current/target*100);if(loading)return <main className="min-h-screen grid place-items-center bg-slate-50 text-slate-500">Loading…</main>;return <main className="min-h-screen bg-slate-50 px-3 py-5 text-slate-900 sm:px-6"><div className="mx-auto max-w-6xl"><header className="flex items-center justify-between"><Link href="/goal" className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Finance · Goal</Link><button onClick={load} className="rounded-xl bg-white p-2.5 shadow-sm ring-1 ring-slate-200"><RefreshCw size={16}/></button></header>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<section className="mt-4 rounded-[1.8rem] bg-white p-5 shadow-sm ring-1 ring-slate-100 sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-slate-400">Financial Goal</p><h1 className="mt-1 text-3xl font-black">Path to {money(target)}</h1><p className="mt-2 text-sm text-slate-500">Your saved target, investments and recurring activity drive this projection.</p></div><div className="grid size-14 place-items-center rounded-2xl bg-violet-50 text-violet-700"><Target/></div></div><div className="mt-6 grid gap-3 sm:grid-cols-4"><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Current</p><p className="mt-1 text-xl font-black">{money(current)}</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Progress</p><p className="mt-1 text-xl font-black">{progress.toFixed(1)}%</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Monthly surplus</p><p className="mt-1 text-xl font-black">{money(surplus)}</p></div><div className="rounded-2xl bg-violet-50 p-4"><p className="text-xs text-violet-600">Target date</p><p className="mt-1 text-xl font-black text-violet-900">{current>=target?"Reached":reach?fmt(reach.date):"Not reached"}</p></div></div></section><section className="mt-4 rounded-[1.8rem] bg-white p-5 shadow-sm ring-1 ring-slate-100 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-slate-400">Projection</p><h2 className="mt-1 text-xl font-black">Growth path</h2></div><span className="inline-flex items-center gap-1.5 rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-bold text-emerald-700"><TrendingUp size={14}/> {AUTO_REINVEST_RATE}%</span></div><div className="mt-5"><Chart points={points} target={target}/></div><p className="mt-3 text-xs leading-5 text-slate-500">Target is editable. Existing rates apply through entered maturity; maturity proceeds and recurring surplus use the 8.1% reinvestment pool. No maturity values are invented.</p></section></div></main>}
