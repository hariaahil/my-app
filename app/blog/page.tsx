import { createClient } from "@supabase/supabase-js";
import Link from "next/link";
const labels={ai:"AI & Technology",stocks:"Stocks & Markets",sports:"Sports"} as const;
export const dynamic="force-dynamic";
export default async function BlogPage(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;const data=url&&key?(await createClient(url,key).from("content_articles").select("id,slug,title,excerpt,category,published_at").eq("status","published").order("published_at",{ascending:false}).limit(30)).data:[];return <main className="min-h-screen bg-white px-4 py-8 text-black sm:px-6"><div className="mx-auto max-w-6xl"><Link href="/" className="text-sm font-bold text-black/60">← TargetBud</Link><div className="mt-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-black/45">Daily content</p><h1 className="mt-2 text-4xl font-black">TargetBud Journal</h1><p className="mt-2 max-w-2xl text-black/55">Original daily articles across AI, stocks and sports, built from current source material.</p></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{(data??[]).map(a=><article key={a.id} className="rounded-3xl border border-black/10 bg-white p-6 shadow-sm"><span className="rounded-full border border-black/10 bg-black/[.03] px-2.5 py-1 text-[11px] font-bold text-black/65">{labels[a.category as keyof typeof labels]??a.category}</span><h2 className="mt-4 text-xl font-black leading-tight">{a.title}</h2>{a.excerpt&&<p className="mt-2 text-sm leading-6 text-black/55">{a.excerpt}</p>}<Link href={`/blog/${a.slug}`} className="mt-5 inline-block text-sm font-bold text-black">Read article →</Link></article>)}{!data?.length&&<div className="rounded-3xl border border-black/10 bg-white p-8 text-sm text-black/50 sm:col-span-2 lg:col-span-3">Daily articles will appear here after the content engine is configured.</div>}</div></div></main>}
