import { createClient } from "@supabase/supabase-js";
import Link from "next/link";
const labels={ai:"AI & Technology",stocks:"Stocks & Markets",sports:"Sports"} as const;
export const revalidate=300;
export default async function BlogPage(){const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {data}=await db.from("content_articles").select("id,slug,title,excerpt,category,published_at").eq("status","published").order("published_at",{ascending:false}).limit(30);return <main className="min-h-screen bg-slate-50 px-4 py-8 text-slate-900 sm:px-6"><div className="mx-auto max-w-6xl"><Link href="/" className="text-sm font-bold text-violet-700">← TargetBud</Link><div className="mt-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-slate-400">Daily content</p><h1 className="mt-2 text-4xl font-black">TargetBud Journal</h1><p className="mt-2 max-w-2xl text-slate-500">Original daily articles across AI, stocks and sports, built from current source material.</p></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{(data??[]).map(a=><article key={a.id} className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-100"><span className="rounded-full bg-violet-50 px-2.5 py-1 text-[11px] font-bold text-violet-700">{labels[a.category as keyof typeof labels]??a.category}</span><h2 className="mt-4 text-xl font-black leading-tight">{a.title}</h2>{a.excerpt&&<p className="mt-2 text-sm leading-6 text-slate-500">{a.excerpt}</p>}<Link href={`/blog/${a.slug}`} className="mt-5 inline-block text-sm font-bold text-violet-700">Read article →</Link></article>)}{!data?.length&&<div className="rounded-3xl bg-white p-8 text-sm text-slate-500 ring-1 ring-slate-100 sm:col-span-2 lg:col-span-3">Daily articles will appear here after the content engine is configured.</div>}</div></div></main>}
